# Production Environment Configuration
# Used for production deployment (do not commit actual values)

# Environment and storage configuration
STORAGE_MODE=aws
ENV=prod

# AWS configuration
AWS_REGION=eu-west-2

# AWS resources (defaults handled by settings.py based on ENV=prod)
# S3_BUCKET=security-assistant-files
# DYNAMODB_TABLE=security-assistant-jobs

# Google GenAI configuration (set via environment variables in Lambda)
# GEMINI_API_KEY=set_via_lambda_environment