# Development AWS Environment Configuration
# Copy to .env for development AWS testing

# Environment and storage configuration
STORAGE_MODE=aws
ENV=dev

# AWS configuration
AWS_PROFILE=design-lee
AWS_REGION=eu-west-2

# AWS resources (defaults handled by settings.py based on ENV=dev)
# S3_BUCKET=security-assistant-dev-445567098699
# DYNAMODB_TABLE=security-assistant-dev-jobs

# Google GenAI configuration
GEMINI_API_KEY=your_key_here